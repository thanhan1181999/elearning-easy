<div class="row">
  <%= render 'userInforAction'%>
  <div class="col-md-9"> 
      <h3 style="text-align:center; padding:10px"><%= @title %></h3> 
      <%= render(@users) || "No one"%>
      <%= will_paginate %> 
  </div>
</div>
