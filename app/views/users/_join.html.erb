<%= form_for Join.new, remote: true do |f|%>
  <%= f.hidden_field :user_id, value: current_user.id%>
  <%= f.hidden_field :course_id, value: @course.id%>
  <%= f.submit "Join course", class: "btn btn-primary"%>
<%end%>